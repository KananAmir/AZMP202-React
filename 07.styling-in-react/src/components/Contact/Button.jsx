import styled from "styled-components";

const ContactButton = () => {
  return <StyledButton>ContactButton</StyledButton>;
};

const StyledButton = styled.button`
  color: orange;
  font-size: 1re;
  border: 2px solid orange;
  padding: 1rem 2rem;
`;
export default ContactButton;
